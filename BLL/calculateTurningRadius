function turningRadius = calculateTurningRadius(car, ackermanAngleInner)
    % Dönüş yarıçapı hesaplaması
    turningRadius = car.wheelBase / tand(ackermanAngleInner); % Derece cinsinden hesaplama
end
